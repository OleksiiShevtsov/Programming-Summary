#include <iostream>
#include <vector>
#include <string>
#include <future>
#include <windows.h>
using namespace std;

int main() {

	int sum = 0;
	vector<int> arr = { 10, 20, 30, 40 };

	for_each(arr.begin(), arr.end(), ) {

	}

	int exit; cin >> exit;
	return 0;
}